<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.coffeeshopapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoffeeShopApp"
        tools:targetApi="31">
        <activity
            android:name=".BrowswListActivity"
            android:exported="false" />
        <!--
        <activity
            android:name=".AddItem"
            android:exported="false" />
        -->
        <!-- SplashActivity -->
        <!-- ENTRY POINT -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainScreenActivity -->
        <activity
            android:name=".MainScreenActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CoffeeShopApp"></activity> <!-- SignUpAndSignInActivity -->
        <activity
            android:name=".SignUpAndSignInActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeShopApp"></activity> <!-- SignUpActivity -->
        <activity
            android:name=".SignUpActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeShopApp"></activity> <!-- SignInActivity -->
        <activity
            android:name=".SignInActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeShopApp"></activity> <!-- CoffeeBrandsListActivity -->
        <activity
            android:name=".CoffeeBrandsListActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeShopApp"></activity> <!-- AddItemsListActivity -->
        <activity
            android:name=".AddItemsListActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeShopApp"></activity> <!-- AddItemActivity -->
        <activity
            android:name=".AddItem"
            android:exported="true"
            android:theme="@style/Theme.CoffeeShopApp"></activity> <!-- CheckoutActivity -->
        <activity
            android:name=".CheckoutActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeShopApp"></activity>
    </application>

</manifest>